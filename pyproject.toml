[project]
name = "sec-smart-dev"
version = "0"
description = "Development tooling for the SEC Smart Home Assistant custom component."
requires-python = ">=3.13"

[dependency-groups]
dev = [
    "openapi-python-client>=0.21",
    "datamodel-code-generator>=0.26",
    "pytest>=8",
    "pytest-asyncio>=0.24",
]

[tool.uv]
package = false
